from setuptools import setup

setup(
    name="board_online",
    version="1.0",
    packages=["boe"],
    package_dir={"": "src"},
    entry_points={
    # 'console_scripts': [
        # ' = ',
    # ],
},
)
